/** @type {import('next').NextConfig} */
const nextConfig = {
  // HAPUS bagian i18n!
  images: {
    domains: ['upload.wikimedia.org', 'uxwing.com'],
  },
  // Fitur lain boleh
}
module.exports = nextConfig;
